# OS
.DS_Store
.AppleDouble
.LSOverride
._*
Thumbs.db
ehthumbs.db
Desktop.ini
$RECYCLE.BIN/

# Ruby
.bundle/
vendor/bundle/
*.gem
*.rbc
/.config

# Reports & screenshots (generated at runtime)
reports/

# Bundler — commit Gemfile.lock after running `bundle install` locally
# Gemfile.lock

# IDE
.idea/
.vscode/
*.iml
*.sublime-project
*.sublime-workspace

# Environment files (keep .env.example tracked, ignore actual secret files)
.env
.env.local
.env.*.local
